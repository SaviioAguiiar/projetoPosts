<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <title>Posts</title>
</head>
<body class="bg-dark">
    <div class="container">
        <h1 class="text-light text-center text-uppercase">Posts</h1>
        <a href="/add" class="btn btn-primary stretched-link mb-3">Adicionar Posts</a> 
        <%for(let i = 0 ; i < links.length ; i++) {%>
            <div class="card p-3 mb-3" id="<%=links[i]._id%>">
                <form class="d-flex justify-content-between align-items-center" method="post" action="/?_method=DELETE">
                    <div class="d-flex flex-column">
                      <h2><%=links[i].title%></h2>
                      <div><i class="text-dark">Descrição: </i><%=links[i].description%></div>
                      <div class="text-dark"><i>Url: </i><%=links[i].url%></div>
                      <div class="text-dark"><i class="material-icons">bar_chart</i> Clicks: <%=links[i].click%></div>
                    </div>
                    <div class="d-flex flex-column">
                       <input value="<%=links[i]._id%>" name="id" hidden>
                       <a class="btn btn-info mb-1" href="/edit/<%=links[i]._id%>">Editar</a>
                       <button class="btn btn-danger" type="submit">Deletar</button> 
                    </div>
                </form> 
            </div>
        <%}%>   
    </div>
</body>
</html>